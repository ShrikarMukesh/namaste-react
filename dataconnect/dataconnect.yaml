specVersion: "v1"
serviceId: "namaste-react"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "namaste-react-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
